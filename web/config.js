// Filled in after the Supabase project is linked. Both values are safe to publish:
// the key is the project's publishable (anon) key, and RLS only allows reads.
// Leave empty to preview against ../data/feed.json (run `npm run ingest` first).
window.SOFIA_EVENTS_CONFIG = {
  supabaseUrl: '',
  supabaseKey: '',
};
